import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    images: {
    domains: ['images.unsplash.com'], // Add this line or modify the existing domains list
  },
};

export default nextConfig;
